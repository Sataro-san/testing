public class RectangleLib {
    public static double getTriangleArea(int a, int h) {
        return a * h / 2;
    }

    public static double getCircleArea(int r) {
        return Math.PI * Math.pow(r, 2);
    }
}
